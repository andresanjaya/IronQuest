
  # Feature Roadmap and Database Schema

  This is a code bundle for Feature Roadmap and Database Schema. The original project is available at https://www.figma.com/design/q10brmr0OTpvWWRYMOx8v8/Feature-Roadmap-and-Database-Schema.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  